# Stellara 실행 및 제출 가이드

> 이 문서는 저장소(Git)를 받은 사람이 프로젝트를 빌드·실행하는 방법과,
> 제출 시 주의할 점을 정리한 문서입니다.

---

## 1. 한눈에 보기

- **프레임워크**: Flutter (Dart SDK `^3.11.1`)
- **백엔드**: Firebase (Auth, Firestore)
- **외부 API**: Prokerala(점성술), Gemini/OpenAI/Anthropic(AI 질문) — *기본 설정에서는 모두 비활성화*
- **중요**: Git만 클론하면 **그대로는 빌드되지 않습니다.** 루트에 `.env` 파일을 만들어야 합니다(아래 3번).

---

## 2. 요구 환경

| 항목 | 버전/조건 |
|------|-----------|
| Flutter SDK | Dart `^3.11.1` 이상 (최신 stable 권장) |
| 실행 대상 | Android 에뮬레이터/기기, 또는 Chrome(웹) |
| Firebase 설정 | `android/app/google-services.json` 이 **이미 저장소에 포함**되어 있어 추가 설정 불필요 |

---

## 3. ⚠️ 가장 중요한 단계 — `.env` 파일 만들기

이 프로젝트는 `pubspec.yaml`에서 `.env`를 **에셋으로 등록**해 두었습니다.
보안상 실제 `.env`는 Git에 올리지 않으므로(`.gitignore` 처리됨), **파일이 없으면 빌드가 실패합니다.**

해결 방법은 간단합니다. 저장소에 함께 들어 있는 `.env.example`을 복사해서 `.env`로 만들면 됩니다.

```bash
# 프로젝트 루트에서
cp .env.example .env
```

기본 설정(`PROKERALA_REMOTE_ENABLED=false`, `AI_REMOTE_ENABLED=false`, `USE_FIXTURE_IN_DEBUG=true`)에서는
**외부 유료 API를 호출하지 않고 내장 샘플 데이터(fixture)로 동작**하므로,
**API 키를 비워둔 채로도 앱이 정상 실행**됩니다. 채점/시연용으로는 이 상태로 충분합니다.

### `.env` 작성 예시

```dotenv
# ── Prokerala (점성술 API) ──────────────────────────────
# 비워두면 원격 호출 없이 내장 샘플로 동작합니다.
PROKERALA_CLIENT_ID=여기에_Client_ID_입력
PROKERALA_CLIENT_SECRET=여기에_Client_Secret_입력
PROKERALA_BASE_URL=https://api.prokerala.com
PROKERALA_TOKEN_URL=https://api.prokerala.com/token
PROKERALA_REMOTE_ENABLED=false   # true 로 바꾸면 실제 API 호출(과금 주의)
USE_FIXTURE_IN_DEBUG=true        # 디버그 시 녹화된 응답 우선 사용

# ── AI 질문 기능 ────────────────────────────────────────
AI_REMOTE_ENABLED=false          # true 로 바꿔야 실제 AI 호출
AI_PROVIDER_DEFAULT=auto
AI_PROVIDER_ORDER=gemini,openai,anthropic

# 실제로 AI 기능을 켜려면 아래에 발급받은 키를 입력하세요.
# Gemini: https://aistudio.google.com/app/apikey (무료 quota 있음)
GEMINI_API_KEY=
AI_MODEL_GEMINI_DEFAULT=gemini-2.5-flash

# OpenAI: https://platform.openai.com/api-keys
OPENAI_API_KEY=
AI_MODEL_OPENAI_DEFAULT=gpt-4o-mini

# Anthropic: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
AI_MODEL_ANTHROPIC_DEFAULT=claude-3-5-haiku-latest
```

> 키 형식 예시(실제 키 아님): Gemini/Firebase는 `AIza...`로 시작, OpenAI는 `sk-...`, Anthropic은 `sk-ant-...` 형태입니다.

---

## 4. 실행 방법

```bash
flutter pub get        # 의존성 설치
cp .env.example .env   # (3번) .env 생성 — 한 번만
flutter run            # 연결된 기기/에뮬레이터에서 실행
# 웹으로 실행하려면:  flutter run -d chrome
```

---

## 5. 제출 관련 — "Git에 올라간 것만으로 교수님이 실행할 수 있나?"

**거의 가능하지만, `.env` 파일 하나는 받는 분이 직접 만들어야 합니다.**

| 필요한 것 | Git 포함 여부 | 비고 |
|-----------|---------------|------|
| 소스 코드 전체 | ✅ 포함 | |
| `android/app/google-services.json` (Firebase 설정) | ✅ 포함 | 그대로 빌드 가능 |
| `.env.example` (환경변수 양식) | ✅ 포함 | |
| **`.env` (실제 환경변수 파일)** | ❌ 미포함(의도적) | **받는 분이 `cp .env.example .env`로 생성 필요** |

→ 따라서 제출 시 이 가이드(또는 README)에 **"`.env`를 만들어야 한다"**는 안내를 꼭 포함하세요.
그렇지 않으면 교수님이 클론 후 바로 `flutter run` 시 에셋 누락으로 빌드가 실패합니다.

---

## 6. `flutter analyze` 결과에 대하여

`flutter analyze` 실행 시 **30개 항목**이 보고되지만, **전부 `info`(스타일 권고) 또는 `warning`(미사용 코드)이며 `error`는 0개**입니다.
즉, **앱 빌드·실행에는 전혀 지장이 없고, 제출을 위해 반드시 고쳐야 하는 것은 없습니다.**

분류하면 다음과 같습니다.

- **미사용 import / 코드 (warning)** — 예: `astrology_screen.dart`, `auth_repository.dart`의 미사용 import, `landing_screen.dart`의 미사용 `_drawGlow`. *지워도 안전하며, 정리하면 가장 깔끔해지는 항목.*
- **deprecated 사용 (info)** — `withOpacity` → `.withValues()`, `dart:html` → `package:web` 권장. *지금도 동작하며 향후 SDK 대비용 권고.*
- **스타일 권고 (info)** — `if` 문 중괄호 권장, 불필요한 밑줄(`__`) 사용 등. *순수 코드 스타일.*

**권장**: 시간 여유가 있으면 *미사용 import/코드(warning)*만 정리하면 analyze가 한결 깨끗해집니다. 나머지 info는 그대로 두어도 무방합니다.

---

## 7. 보안 점검 결과 (Git에 민감정보가 올라갔는지)

전체 추적 파일과 **커밋 히스토리 전체**를 점검한 결과는 다음과 같습니다.

### ✅ 안전 — 노출되지 않음
- **AI 제공사 키(OpenAI `sk-...`, Anthropic `sk-ant-...`)**: 코드/히스토리 어디에도 없음.
- **Prokerala Client ID/Secret**: 하드코딩 없음. `env.dart`는 전부 `.env`에서 읽도록 작성됨.
- **`.env` 파일 자체**: 한 번도 커밋된 적 없음(`.gitignore` 정상 동작).

### ⚠️ 참고 — 노출되어 있으나 일반적으로 허용되는 항목
- **Firebase 설정값** (`google-services.json`의 `AIzaSy...` API 키, `project_id: stellara-11878`)이 저장소에 포함되어 있습니다.
  - Google 공식 가이드상 이 키는 **"비밀(secret)이 아니며"** 클라이언트 앱에 포함·공개되도록 설계된 식별자입니다. 실제 접근 제어는 **Firestore 보안 규칙**(이 프로젝트는 `firestore.rules`에서 로그인 사용자만 허용하도록 설정됨)과 App Check로 이뤄집니다.
  - 다만 저장소가 **공개(public)** 상태라면, 안전성을 높이기 위해 권장:
    1. Google Cloud Console에서 해당 API 키를 **앱 패키지명(`com.stellara.app`) + SHA 지문**으로 제한.
    2. Firebase **App Check** 활성화.
    3. 채점이 끝난 뒤에도 걱정되면 키를 재발급(rotate).
- 참고: 정리되지 않은 중복 파일 `android/app/google-services_.json`(밑줄 버전)이 함께 추적되고 있습니다. 불필요하면 삭제 권장.

### 결론
**진짜 비밀키(AI 키, Prokerala secret, `.env`)는 노출되지 않았습니다.** 제출해도 민감정보 유출 위험은 낮습니다.
공개 저장소라면 위 ⚠️ 항목(Firebase 키 제한)만 점검하면 더욱 안전합니다.
